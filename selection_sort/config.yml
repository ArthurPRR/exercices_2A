name: selection_sort
tags: array int sorting
description: "Sort an array in ascending order using selection sort (O(n²) algorithm)."
timeout: 3
distributes:
  - test.o
  - skeleton.c
  - Makefile
commands:
  prepare: "make prepare"
  validate: "make test"
