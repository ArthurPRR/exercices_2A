#include <stdio.h>
#include <stdlib.h>

// Return the maximum sum of a contiguous subarray.
// If the input is invalid (len <= 0), return 0.

int max_subarray(int len, int arr[len]) {
    (void)len;
    (void)arr;
    return 0;
}
