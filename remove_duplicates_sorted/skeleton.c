#include <stdio.h>
#include <stdlib.h>

// Remove duplicates from a sorted array in place.
// Return the new length of the array, or 0 if the input is invalid.

int remove_duplicates_sorted(int len, int arr[len]) {
    (void)len;
    (void)arr;
    return 0;
}
