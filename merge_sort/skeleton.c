#include <stdio.h>
#include <stdlib.h>

// Sort the array 'arr' of length 'len' in ascending order using merge sort.
// If the input is invalid (len < 0), do nothing.

void merge_sort(int len, int arr[len]) {
    (void)len;
    (void)arr;
}
