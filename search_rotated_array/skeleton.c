#include <stdio.h>
#include <stdlib.h>

// Search for 'target' in a sorted array rotated at an unknown pivot.
// Return its index if found, -1 otherwise.

int search_rotated_array(int len, int arr[len], int target) {
    (void)len;
    (void)arr;
    (void)target;
    return -1;
}
