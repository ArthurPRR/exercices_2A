#include <stdio.h>
#include <stdlib.h>

// Search for the target value in the array 'arr' of length 'len'.
// Return the index of the first occurrence of 'target' if found, -1 if not found or if input is invalid.

int linear_search(int len, int arr[len], int target) {
    (void)len;
    (void)arr;
    (void)target;
    return -1;
}
