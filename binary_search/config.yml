name: binary_search
tags: array int search
description: "Find the index of a target value in a sorted array using binary search. Return -1 if not found."
timeout: 3
distributes:
  - test.o
  - skeleton.c
  - trace.h
  - Makefile
commands:
  prepare: "make prepare"
  validate: "make check"
