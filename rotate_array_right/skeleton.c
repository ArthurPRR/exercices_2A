#include <stdio.h>
#include <stdlib.h>

// Rotate the array 'arr' of length 'len' to the right by 'k' positions.
// If the input is invalid (len < 0), do nothing.

void rotate_array_right(int len, int arr[len], int k) {
    (void)len;
    (void)arr;
    (void)k;
}
